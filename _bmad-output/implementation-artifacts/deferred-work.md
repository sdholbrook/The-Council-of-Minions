# Deferred Work

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-handle-zero-item-and-multi-item-extraction.md`
  summary: The four pre-existing sibling steps in `council-mvp-local-validate.ps1` (tenant packet, manual slice, Outlook slice, 1.3 extraction slice) throw on a child validator's non-zero exit before echoing its output, so the child's issue list is swallowed on exactly the runs where it matters.
  evidence: Reproduced during the story 1.4 review — a failing child validator's diagnostics never appear in suite output because `throw` precedes `$output | ForEach-Object { Write-Host $_ }`; the new 1.4 step already prints first and can serve as the template.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-handle-zero-item-and-multi-item-extraction.md`
  summary: The story 1.1/1.2/1.3 slice validators' `Add-Issue` lacks `[AllowEmptyCollection()]`, so on any failing slice the first `Add-Issue` call crashes with a parameter-binding error instead of listing the violations (exit code still non-zero, diagnostics lost).
  evidence: Demonstrated during the story 1.4 review against a mutated copy of the 1.3 slice; the 1.4 validator carries the one-attribute fix at `zero-multi-item-extraction-slice-validate.ps1:13`, and backporting was out of scope because the spec froze the 1.1–1.3 validators.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-handle-zero-item-and-multi-item-extraction.md`
  summary: No committed artifact exercises any slice validator's failure path; negative (mutation) tests exist only as ad-hoc scratchpad runs, so a mistyped property name could silently neuter a check while the suite stays green.
  evidence: PowerShell returns `$null` for absent properties without error, so a validator check can decay invisibly; all Epic 1 validators (1.1 through 1.4) share this gap, and the 1.4 review's 12-mutation probe set would be a ready seed for a committed negative-test harness.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-handle-source-drift-and-supersession.md`
  summary: The committed-negative-test gap previously scoped to the 1.1-1.4 slice validators now also covers the story 1.5 drift/supersession validator, whose enforcement checks are exercised only by ephemeral scratchpad mutation runs, so a future edit could silently neuter drift-contract enforcement while the canonical suite stays green.
  evidence: The aggregate suite asserts only exit 0 plus the success marker against the committed slice; the 1.5 review's 20-probe mutation set (6 implementation-pass, 3 acceptance-criteria, 11 hardening probes) ran only in scratchpad and left nothing committed, and the existing ledger entry explicitly scopes the gap to "all Epic 1 validators (1.1 through 1.4)".
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-handle-source-drift-and-supersession.md`
  summary: All four 1.1-1.4 slice validators check required fields with `.Contains(...)` on `PSObject.Properties.Name`, which degrades to substring matching when a record has exactly one property, so a lone misnamed field containing a required field's name as a substring would pass.
  evidence: The 1.5 validator's review replaced this exact pattern with the `-contains` operator (Story 1.5 Review Triage Log); the latent copies sit at `zero-multi-item-extraction-slice-validate.ps1:48`, `manual-source-record-slice-validate.ps1:175`, `outlook-source-reference-slice-validate.ps1:174`, and `proposed-work-item-extraction-slice-validate.ps1:174`, and the 1.5 spec forbids editing 1.1-1.4 validators, so the fix cannot land in this story.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-7-validator-proven-to-fail.md`
  summary: The tenant-readiness validator's mutation battery pins every slice-reachable check to committed fixture patterns, but the battery's own veto branches (fixtures-dir missing, canonical fixture missing, expectedIssuePatterns missing/too-broad, zero-issue and unmatched-pattern exits) are exercised by no committed automation, so a future edit could disable the battery itself while `council-mvp-local-validate.ps1` (exit-0 + OK-token assertions only) stays green.
  evidence: Repo-wide search during the 5-7 review found no committed executor of the validator's failure paths; two independent review passes demonstrated that inverting the battery's exit conditions in a copy produces byte-identical green output from every committed consumer. AC-level negative proofs exist only as manual commands in the spec's Verification section — the same scratchpad-evaporation disease this story fixed one level down.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-7-validator-proven-to-fail.md`
  summary: The input-decay tripwires over independent facts ("Independent fact ... is missing or empty" for manifest target and decision-packet values) are structurally unpinnable by the battery, because fixtures can only substitute the tenant-slice path, never the manifest/packet paths; a battery extension substituting a corrupted manifest/packet copy could pin them.
  evidence: The check-set function already takes -ManifestPath/-TenantDecisionPacketPath parameters, so a committed corrupted-manifest probe is mechanically possible; today those tripwires fail closed at runtime but their own regression would be invisible to the battery (5-7 review pass 2, blind-hunter finding).
- source_spec: `_bmad-output/implementation-artifacts/spec-5-7-validator-proven-to-fail.md`
  summary: `council-mvp-local-validate.ps1:306` invokes the tenant-readiness step via `powershell` with a Windows-style backslash script path, which resolves on the Linux host only through a `~/.local/bin/powershell` shim; on a host without the shim the suite step breaks for environmental, not contractual, reasons.
  evidence: Observed during the 5-7 review (verification-gap reviewer traced the suite's invocation); pre-existing behavior in a file outside the 5-7 owned paths, so the fix could not land in this story.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-7-validator-proven-to-fail.md`
  summary: The suite's sprint-status tripwires (`council-mvp-local-validate.ps1` Sprint status YAML check) prove cardinality only, not membership — story lines are counted via the `'^  [0-9]+-[0-9]+-'` shape regex against exact totals with no expected-key list, so replacing a real story key with a fabricated same-shape key (e.g. `9-9-fake: done`) keeps every suite check green while a story silently vanishes from tracking.
  evidence: Pre-existing at the 5-7 baseline (the check has never carried a key roster); surfaced by the 5-7 follow-up review's adversarial pass while the counts were being repaired 5/25/5 → 6/32/6. `sprint-status.yaml` story keys are enumerable from the epics files, so a committed expected-key manifest (or a keys-harvest cross-check against `_bmad-output/planning-artifacts` epics) would close it without weakening the exact-count design.
